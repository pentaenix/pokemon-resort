# Transfer Docs

Purpose: cross-save movement, OpenHome identity ownership, transfer orchestration.

Owned modules: `src/transfer/*`, `include/transfer/*`, relevant bridge adapters.

Dependencies: save adapters/persistence/bridge tooling.

Extension points: use-case services, adapter implementations.

Tests: existing transfer + resort/openhome tests and new contract tests.
