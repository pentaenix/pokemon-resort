# Unit Tests

Use this folder for fast tests that do not need the full app runtime or real packaging artifacts.

Current bridge unit tests cover:

- missing command arguments
- missing save file probe behavior
- probe JSON compatibility fields
- import command missing-file behavior
- write-projection command argument validation
